# Build outputs
build/
build-*/
dist/
*.tar.gz
*.sha256

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store

# Temporary files
*.log
.cache/

# npm
node_modules/
package-lock.json
# Staged binaries in platform packages (written by update_packaging.sh, not
# source code — each release overwrites them, and a 52K binary per release
# bloats the repo for zero archival value).
packaging/npm/platforms/*/bin/

# Qualcomm proprietary (should never be committed)
genai_lib/
utilities/
qnn_model_prepare_*.py
libGenie*.so
libQnn*.so
libSnpe*.so
*.serialized.bin
